# RS-HTTP

A basic HTTP server written in Rust, serving static files from a specified directory. It supports persistent connections on HTTP/1.1.

Capable of handling just over 100k requests per second on my Ryzen 9 7900X.
